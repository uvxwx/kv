Todo

Knowledge
1. eng::Mutex protects metadata.

v2
1. TTL
2. waitForChange

v3
1. Bound Store::locks size by removing unused locks: implement safe erasure.

v4
1. Graph-based deadlock detector?

?
1. reorder fields in structs to save space
2. Faster hash tables: abseil, boost, ...
3. ConcurrentVariable instead of `unique_lock`s
